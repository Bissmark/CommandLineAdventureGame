#pragma once
#include <iostream>
#include <algorithm>
using namespace std;



class Fight {
    public:
        Fight();
        // I want a UI for what the player will do within the fight, we can work it off pokemon to begin with
        void FightMenu();
    private:
};